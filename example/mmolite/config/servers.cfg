[master server]
setup=-s;--setup;Setup master server;;;False
master_port=-p;--port;Master server's listening port number;PORT;int;5000

[character server]
setup=-s;--setup;Setup character server;;;False
master_ip=-i;--ip;Master server's ip address;IP;string;127.0.0.1
master_port=-p;--port;Master server's port number;PORT;int;5000
username=-u;--username;Master server's login name;USERNAME;string;Character
password=-w;--password;Master server's login password;PASSWORD;string;retcarahc

[world server]
setup=-s;--setup;Setup world server;;;False
master_ip=-i;--ip;Master server's ip address;IP;string;127.0.0.1
master_port=-p;--port;Master server's port number;PORT;int;5000
username=-u;--username;Master server's login name;USERNAME;string;World
password=-w;--password;Master server's login password;PASSWORD;string;dlrow
world_port=-o;--wport;World server's listening port number;PORT;int;5002

[cluster server]
setup=-s;--setup;Setup cluster server;;;False
master_ip=-i;--ip;Master server's ip address;IP;string;127.0.0.1
master_port=-p;--port;Master server's port number;PORT;int;5000
username=-u;--username;Master server's login name;USERNAME;string;Cluster
password=-w;--password;Master server's login password;PASSWORD;string;retsulc
cluster_port=-o;--lport;Cluster server's listening port number;PORT;int;5003

[zone 1 server]
setup=-s;--setup;Setup zone server;;;False
master_ip=-i;--ip;Master server's ip address;IP;string;127.0.0.1
master_port=-p;--port;Master server's port number;PORT;int;5000
username=-u;--username;Master server's login name;USERNAME;string;Zone
password=-w;--password;Master server's login password;PASSWORD;string;enoz
zone_port=-o;--zport;Zone server's listening base port number;PORT;int;6000
zone_id=-z;--zone;Zone id;ZONE;int;1
