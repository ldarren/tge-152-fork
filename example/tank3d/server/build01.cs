datablock StaticShapeData(Build01)
{
          category = "Misc";
          shapeFile = "~/data/shapes/Building/build01.dts";
			computeCRC = true;
};

function Build01::onAdd(%this,%obj)
{
        // %obj.playThread(0,"Action");
}
