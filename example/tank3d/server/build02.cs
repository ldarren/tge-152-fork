datablock StaticShapeData(Build02)
{
          category = "Misc";
          shapeFile = "~/data/shapes/Building/build02.dts";
			computeCRC = true;
};

function Build02::onAdd(%this,%obj)
{
        // %obj.playThread(0,"Action");
}
